services:
  artworld-api:
    image: ${ECR_REGISTRY}/${ECR_REPOSITORY}:${IMAGE_TAG}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: artworld-api
    networks:
      - internal

networks:
  internal:
    driver: bridge
  artworld-network:
    external: true
